import * as React from "react";
import "./index.less";

export default function CheckInspectDiff(props: any) {
  return (
    <span className="comp-check-inspect">{`View Inspection Result >`}</span>
  );
}
