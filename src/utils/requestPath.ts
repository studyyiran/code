export const foundation = (url: string) => {
  return `/foundation-data-center${url}`
}

export const user = (url: string) => {
  return `/user-center${url}`
}

export const business = (url: string) => {
  return `/recycle-business-center${url}`
}